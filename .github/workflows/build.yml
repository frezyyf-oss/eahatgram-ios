name: Build eahatgram

on:
  push:
    branches: [master, main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 180

    steps:
      - name: Checkout eahat changes
        uses: actions/checkout@v4
        with:
          path: eahat-changes
          submodules: false

      - name: Clone Telegram-iOS with submodules
        run: |
          git clone --recurse-submodules --shallow-submodules --depth 1 \
            https://github.com/TelegramMessenger/Telegram-iOS.git telegram-ios

      - name: Apply eahatgram modifications
        run: |
          SRC=eahat-changes
          DST=telegram-ios

          cp "$SRC/submodules/SettingsUI/Sources/EahatGramStats.swift"            "$DST/submodules/SettingsUI/Sources/"
          cp "$SRC/submodules/SettingsUI/Sources/EahatGramController.swift"       "$DST/submodules/SettingsUI/Sources/"
          cp "$SRC/submodules/SettingsUI/Sources/EahatGramStatsController.swift"  "$DST/submodules/SettingsUI/Sources/"
          cp "$SRC/submodules/SettingsUI/Sources/EahatGramVisualController.swift" "$DST/submodules/SettingsUI/Sources/"
          cp "$SRC/submodules/SettingsUI/Sources/EahatGramNFTController.swift"    "$DST/submodules/SettingsUI/Sources/"
          cp "$SRC/submodules/SettingsUI/Sources/EahatGramRatingController.swift" "$DST/submodules/SettingsUI/Sources/"
          cp "$SRC/build-system/appstore-configuration.json"                      "$DST/build-system/"
          cp "$SRC/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoSettingsItems.swift" \
             "$DST/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/"
          cp "$SRC/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoScreen.swift" \
             "$DST/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/"
          cp "$SRC/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoScreenSettingsActions.swift" \
             "$DST/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/"
          cp "$SRC/submodules/TelegramUI/Sources/ChatController.swift" \
             "$DST/submodules/TelegramUI/Sources/"

      - name: Select Xcode
        run: |
          XCODE=$(ls /Applications/ | grep -E '^Xcode_26' | sort -V | tail -1)
          if [ -z "$XCODE" ]; then
            XCODE=$(ls /Applications/ | grep -E '^Xcode_' | sort -V | tail -1)
          fi
          echo "Using: $XCODE"
          sudo xcode-select -s "/Applications/$XCODE"
          xcodebuild -version

      - name: Install Bazelisk
        run: brew install bazelisk

      - name: Restore Bazel cache
        uses: actions/cache@v4
        with:
          path: ~/telegram-bazel-cache
          key: bazel-${{ runner.os }}-${{ hashFiles('telegram-ios/WORKSPACE') }}
          restore-keys: bazel-${{ runner.os }}-

      - name: Set up signing identity and provisioning profiles
        run: |
          # Single self-signed cert used for both CMS-signing the profile and app codesigning
          openssl req -x509 -newkey rsa:2048 \
            -keyout /tmp/sign_key.pem -out /tmp/sign_cert.pem \
            -days 365 -nodes \
            -subj "/CN=iPhone Developer: eahat (C67CF9S4VU)/O=eahat/C=US" 2>/dev/null

          CERT_B64=$(openssl x509 -in /tmp/sign_cert.pem -outform DER | base64 | tr -d '\n')

          openssl pkcs12 -export \
            -inkey /tmp/sign_key.pem -in /tmp/sign_cert.pem \
            -out /tmp/signing.p12 -passout pass:"" 2>/dev/null

          # SYSTEM keychain — Bazel sandbox strips HOME so user keychain is invisible to actions
          sudo security import /tmp/signing.p12 -P "" \
            -k /Library/Keychains/System.keychain \
            -T /usr/bin/codesign -T /usr/bin/security 2>/dev/null
          sudo security set-key-partition-list -S apple-tool:,apple: -s -k "" \
            /Library/Keychains/System.keychain 2>/dev/null || true
          # Also trust the cert so security cms -D can decode the signed profile
          sudo security add-trusted-cert -d -r trustRoot \
            -k /Library/Keychains/System.keychain /tmp/sign_cert.pem

          # User keychain so security cms -S can sign
          security create-keychain -p "" /tmp/build.keychain 2>/dev/null || true
          security unlock-keychain -p "" /tmp/build.keychain
          security import /tmp/signing.p12 -P "" -k /tmp/build.keychain \
            -T /usr/bin/security 2>/dev/null
          security set-key-partition-list -S apple-tool:,apple: -s -k "" /tmp/build.keychain 2>/dev/null || true
          security list-keychains -d user -s /tmp/build.keychain \
            $(security list-keychains -d user | tr -d '"')

          # Profile plist — DeveloperCertificates has our cert DER so rules_apple finds the identity.
          # app-groups entitlement is CRITICAL: without it Telegram can't open its shared database
          # and will show a black screen forever.
          cat > /tmp/profile.plist << PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>AppIDName</key><string>eahat</string>
            <key>ApplicationIdentifierPrefix</key><array><string>C67CF9S4VU</string></array>
            <key>TeamIdentifier</key><array><string>C67CF9S4VU</string></array>
            <key>Platform</key><array><string>iOS</string></array>
            <key>IsXcodeManaged</key><true/>
            <key>Name</key><string>eahat dev</string>
            <key>UUID</key><string>EAHAT000-0000-0000-0000-000000000001</string>
            <key>Version</key><integer>1</integer>
            <key>TimeToLive</key><integer>365</integer>
            <key>CreationDate</key><date>2026-01-01T00:00:00Z</date>
            <key>ExpirationDate</key><date>2027-12-31T00:00:00Z</date>
            <key>DeveloperCertificates</key><array><data>$CERT_B64</data></array>
            <key>Entitlements</key><dict>
              <key>application-identifier</key><string>C67CF9S4VU.ph.telegra.Telegraph</string>
              <key>keychain-access-groups</key><array><string>C67CF9S4VU.*</string></array>
              <key>com.apple.security.application-groups</key><array>
                <string>group.ph.telegra.Telegraph</string>
              </array>
              <key>get-task-allow</key><true/>
              <key>aps-environment</key><string>development</string>
            </dict>
          </dict></plist>
          PLIST

          security cms -S -N "iPhone Developer: eahat (C67CF9S4VU)" \
            -k /tmp/build.keychain \
            -i /tmp/profile.plist \
            -o /tmp/fake.mobileprovision
          echo "Profile: $(wc -c < /tmp/fake.mobileprovision) bytes"
          security find-identity -v /Library/Keychains/System.keychain

      - name: Patch BuildConfiguration.py to copy CMS profiles
        working-directory: telegram-ios
        run: |
          python3 - << 'PYEOF'
          path = 'build-system/Make/BuildConfiguration.py'
          src = open(path).read()
          old_fn = '    def copy_profiles_to_destination(self, destination_path):\n        pass'
          new_fn = (
              '    def copy_profiles_to_destination(self, destination_path):\n'
              '        import os as _os, shutil as _sh\n'
              '        _os.makedirs(destination_path, exist_ok=True)\n'
              '        for _n in ["Telegram","Share","NotificationService","NotificationContent","Widget","BroadcastUpload","Intents"]:\n'
              '            _sh.copy("/tmp/fake.mobileprovision", _os.path.join(destination_path, _n + ".mobileprovision"))'
          )
          assert old_fn in src, 'Pattern not found in BuildConfiguration.py'
          open(path, 'w').write(src.replace(old_fn, new_fn))
          print('Patched BuildConfiguration.py')
          PYEOF

      - name: Build debug_arm64
        working-directory: telegram-ios
        run: |
          python3 build-system/Make/Make.py \
            --overrideXcodeVersion \
            --cacheDir "$HOME/telegram-bazel-cache" \
            build \
            --continueOnError \
            --configurationPath build-system/appstore-configuration.json \
            --xcodeManagedCodesigning \
            --buildNumber="${{ github.run_number }}" \
            --configuration=debug_arm64

      - name: Re-sign IPA with correct entitlements for TrollStore
        working-directory: telegram-ios
        run: |
          IPA="bazel-bin/Telegram/Telegram.ipa"
          if [ ! -f "$IPA" ]; then
            echo "IPA not found — listing bazel-bin/Telegram:"
            ls bazel-bin/Telegram/ 2>/dev/null || true
            exit 1
          fi

          # These entitlements must be present for Telegram to initialise on device.
          # Without com.apple.security.application-groups the app can't open its
          # shared database container and shows a black screen on launch.
          cat > /tmp/ents.plist << 'ENT'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>application-identifier</key><string>C67CF9S4VU.ph.telegra.Telegraph</string>
            <key>keychain-access-groups</key><array>
              <string>C67CF9S4VU.ph.telegra.Telegraph</string>
              <string>C67CF9S4VU.*</string>
            </array>
            <key>com.apple.security.application-groups</key><array>
              <string>group.ph.telegra.Telegraph</string>
            </array>
            <key>get-task-allow</key><true/>
            <key>aps-environment</key><string>development</string>
          </dict></plist>
          ENT

          WORK=$(mktemp -d)
          cp "$IPA" "$WORK/orig.ipa"
          cd "$WORK"
          unzip -q orig.ipa

          # Re-sign inner-to-outer: dylibs → frameworks → extensions → main app
          find Payload -name "*.dylib" | sort -r | while read f; do
            codesign --force --sign - "$f" 2>/dev/null || true
          done
          find Payload -type d -name "*.framework" | sort -r | while read f; do
            codesign --force --sign - "$f" 2>/dev/null || true
          done
          find Payload -type d -name "*.appex" | sort -r | while read f; do
            codesign --force --sign - --entitlements /tmp/ents.plist "$f" 2>/dev/null || true
          done
          codesign --force --sign - --entitlements /tmp/ents.plist Payload/*.app

          zip -qr "$GITHUB_WORKSPACE/telegram-ios/eahatgram-device.ipa" Payload
          echo "IPA ready: $(du -sh "$GITHUB_WORKSPACE/telegram-ios/eahatgram-device.ipa")"

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: eahatgram-device-${{ github.run_number }}
          path: telegram-ios/eahatgram-device.ipa
          if-no-files-found: error
          retention-days: 14
